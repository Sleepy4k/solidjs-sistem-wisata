import { APP_NAME, BACKEND_URL } from "./app.config";
import { EMAIL_KEY_PREFIX, TOKEN_KEY_PREFIX } from "./storage.config";
import nprogressConfig from "./nprogress.config";

export { APP_NAME, BACKEND_URL, EMAIL_KEY_PREFIX, TOKEN_KEY_PREFIX, nprogressConfig };