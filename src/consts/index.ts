import {
  APP_ENV,
  APP_NAME,
  BACKEND_URL
} from "./app.config";
import {
  EMAIL_KEY_PREFIX,
  TOKEN_KEY_PREFIX
} from "./storage.config";

export {
  APP_ENV,
  APP_NAME,
  BACKEND_URL,
  EMAIL_KEY_PREFIX,
  TOKEN_KEY_PREFIX
};
